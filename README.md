# Kalyan Ram — Portfolio (Phase 1)

Phase 1 setup for a professional dark portfolio using Vite, React, Tailwind, and Framer Motion.

What is included:
- Vite + React scaffold
- Tailwind CSS + PostCSS config
- Framer Motion variants and useInView helper
- Global styles with CSS variables, noise overlay, and font imports
- App.jsx with AnimatePresence page fade-in
- Placeholder shared components (Navbar, CustomCursor, ScrollProgress, ProjectCard)

Next steps (Phase 2+ when confirmed): implement Navbar behavior, Hero section, and the rest of components incrementally per the execution plan.
